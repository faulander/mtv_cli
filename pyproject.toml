[project]
name = "mtv-cli"
version = "9"
description = "Mediathekview auf der Kommandozeile"
license = "GPL-3.0"
requires-python = ">=3.9"
dependencies = [
    "bottle>=0.12",
    "loguru>=0.7",
    "pick>=2.0",
]
